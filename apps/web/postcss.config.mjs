/**
 * PostCSS config — ESM (.mjs requires `export default`, not `module.exports`)
 * https://postcss.org/docs/postcss-config
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
