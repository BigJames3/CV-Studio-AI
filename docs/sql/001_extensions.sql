-- CV Studio AI — Extensions (run before / with first migrate)
-- PostgreSQL 16+

CREATE EXTENSION IF NOT EXISTS citext;
CREATE EXTENSION IF NOT EXISTS pgcrypto;
-- Optional for future: CREATE EXTENSION IF NOT EXISTS pg_stat_statements;
